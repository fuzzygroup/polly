<%= form_with(model: flier) do |form| %>
  <% if flier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(flier.errors.count, "error") %> prohibited this flier from being saved:</h2>

      <ul>
        <% flier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: "form-label"  %>
    <%= form.text_field :name, { class: "form-select" } %>
  </div>

  <div class="mb-3">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>
  
  <div class="mb-3">
    <%= form.label :flier_type, class: "form-label" %>
    <%= form.select :flier_type, @flier_types, {}, { class: "form-select" } %>
  </div>

  <!-- <div>
    <%#= form.label :group_id, style: "display: block" %>
    <%#= form.text_field :group_id %>
  </div>
 -->
  <!-- <div class="mb-3">
    <%#= form.label :project_id, class: "form-label"%>
    <%#= form.text_field :project_id %>
  </div> -->
  
  <div class="mb-3">
    <%= form.label :project_id, class: "form-label" %>
    <%= form.select :project_id, options_from_collection_for_select(@projects, :id, :name), {}, { class: "form-select" } %>
  </div>

  <div class="mb-3">
    <%= form.submit %>
  </div>
<% end %>
