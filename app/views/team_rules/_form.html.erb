<%= form_with(model: team_rule) do |form| %>
  <% if team_rule.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(team_rule.errors.count, "error") %> prohibited this team_rule from being saved:</h2>

      <ul>
        <% team_rule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :team_id, style: "display: block" %>
    <%= form.text_field :team_id %>
  </div>

  <div>
    <%= form.label :rule_id, style: "display: block" %>
    <%= form.text_field :rule_id %>
  </div>

  <div>
    <%= form.label :active, style: "display: block" %>
    <%= form.checkbox :active %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
