<%= form_with(model: poll_response) do |form| %>
  <% if poll_response.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(poll_response.errors.count, "error") %> prohibited this poll_response from being saved:</h2>

      <ul>
        <% poll_response.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :poll_id, style: "display: block" %>
    <%= form.text_field :poll_id %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
