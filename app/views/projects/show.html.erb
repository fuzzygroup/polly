<p style="color: green"><%= notice %></p>

<%= render @project %>

<div>
  <%= link_to 'Upvote', upvote_project_path(@project), method: :put %> | 
  <%= link_to 'Downvote', downvote_project_path(@project), method: :put %> |
  
  <%= link_to "Edit this project", edit_project_path(@project) %> |
  <%= link_to "Back to projects", projects_path %>

  <%#= button_to "Destroy this project", @project, method: :delete, data: { confirm: 'Are you sure you want to delete this project?' } %>
  
  <%#= link_to 'Delete', project_path(@project), method: :delete, data: { confirm: 'Are you sure you want to delete this post?' } %>
  <%#= link_to 'Delete', project_path(@project), 
              method: :delete, 
              data: { turbo_confirm: 'Are you sure you want to delete this?' } %>
  
</div>
